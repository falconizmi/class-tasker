import { FC } from "react";
import { Activity } from "./ActivityList";

type ActivitySingleProps = {
    activity: Activity
};


export const ActivitySingle: FC<ActivitySingleProps> = (props) => {
    return ()
};
